name: Test

on:
  push:
    branches:
      - master

jobs:
  test:

    runs-on: obuntu-latest

    steps:
    - name: Install go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15
    
    - uses: actions/checkout@v2

    - name: Test
      run: go test ./..